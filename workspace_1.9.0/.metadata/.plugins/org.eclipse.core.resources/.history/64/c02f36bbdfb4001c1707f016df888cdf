/*
 * generalTypes.c
 *
 *  Created on: Mar 29, 2022
 *      Author: josec
 */

#include "generalTypes.h"

int I_CreateInstructionStruct(uint8_t instCode, uint8_t time, MovementInstruction_t* pMovOut) {
	// Comprueba si el codigo de instruccion es valido
	if(instCode < NUM_INSTRUCTIONS && time < I_MAX_TIME_EXECUTION) {
		MovementInstruction_t* pMov = malloc(sizeof(MovementInstruction_t));
		pMov->instruction = instCode;
		pMov->duration = time;
		pMovOut = pMov;
	}
	// Si no devuelve una instruccion NOPE
	else {

	}
}
